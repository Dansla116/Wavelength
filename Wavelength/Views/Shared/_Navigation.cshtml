@{ 
    Layout = null;
}

<script>
    var application = {};
    application.controllers = {};
    application.controllers.Home = {}

    application.controllers.Cards = {}
    application.controllers.Cards.ReadAll = "@Url.Action("ReadAll", "Cards")";
    application.controllers.Cards.Create = "@Url.Action("Create", "Cards")";
    
    application.controllers.Play = {}
    application.controllers.Play.Join = "@Url.Action("Join", "Play")";
    application.controllers.Play.NewGame = "@Url.Action("NewGame", "Play")";
    application.controllers.Play.NewRound = "@Url.Action("NewRound", "Play")";
</script>